require([
    'dojo/parser',
    'dojo/ready',
    'modules/clock/Clock',
    // 'dijit/registry'
], function (parser, ready, Clock) {
    // ready(function(){
    //     parser.parse();
    // });

    // new Clock();
});
